<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SQLiteD"
             x:Class="SQLiteD.MainPage">

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10" Spacing="10">
                <Label Text="EQUIPOS COMPUTO" 
                Font="24"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                FontAttributes="Bold" />
            <Entry x:Name="marcaEntry"
                Placeholder="Marca" />
            <Entry x:Name="ModeloEntry"
                Placeholder="Modelo" />
            <Entry x:Name="SerieEntry"
                Placeholder="Serie"/>
            <Button x:Name="agregarButton"
                Text="Agregar"
                BackgroundColor="Navy"
                TextColor="White" />
             <ListView x:Name="listaListView" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>



</ContentPage>
